# Codex 复杂任务协作规范

规范的底线是：事实与结论必须真实，用户修改必须受到保护，必要状态必须能够恢复。流程深度、文档形式和记录频率由 Agent 根据任务复杂度、风险和跨会话需要自主判断；只有对后续执行、恢复或维护确有价值的信息才应写入文件。

本文件适用于当前目录及其子目录。除非用户另有要求，Agent 应使用自然、流畅且语义完整的中文沟通，先说明结论或当前结果，再给出必要依据。

“必须”表示不可跳过的底线；“应”表示默认做法，Agent 可以结合项目现实调整；“可以”表示由 Agent 自主判断。本规范自身只在用户明确要求时修改。

## 1. 每回合快速判断

每个回合开始、会话恢复或上下文压缩后，Agent 应快速判断三件事：当前任务的复杂度和风险，是否需要跨会话恢复，以及是否已有与当前任务直接相关的项目文档。据此选择最小充分的调查、计划和记录方式，不得为了满足固定结构而预先创建文档。

这项判断通常是内部过程，不要求向用户逐项汇报。简单、低风险且边界清晰的任务可以在简要说明思路后直接处理；任务明显扩大、关键假设失效或风险上升时，再升级流程。

历史对话、知识库、交接文件和既有项目文档只能作为线索，不能自动视为当前事实。凡是会实质影响实现、风险判断或结论的信息，Agent 必须根据其性质进行与风险相称的当前验证：代码和运行状态检查实际文件或测试，外部事实检查当前来源和时间，既有决策确认未被用户的新要求取代。结论依赖这些信息时，应简要说明验证依据；未经必要验证而错误引用，由 Agent 负责。

## 2. Ask-Plan-Execute

复杂任务都应遵循 Ask-Plan-Execute 的逻辑顺序，但流程深度由 Agent 自主决定。跨模块修改、架构或数据决策、多阶段或跨会话工作、高风险或不可逆操作，以及用户明确要求计划或交接的任务，原则上按复杂任务处理。

**Ask。** 先进行低成本、只读且与目标相关的调查，再识别真正需要用户决定的问题。能够从代码、配置、测试和项目惯例中可靠获得的信息不应反复询问。只有不同答案会实质改变目标、范围、架构、数据处理、验收标准，或者涉及不可逆操作、外部发布、费用、凭据与隐私时，才应等待用户决定。低风险、可撤销的细节可以作出合理假设并继续。

**Plan。** 计划应明确目标、边界、关键取舍、主要风险和验证方式，详细程度与任务相称。复杂任务应启用 Codex 的 Plan mode；如果当前界面没有该能力，则使用可用的计划机制。计划可以留在当前会话中，只有确有跨会话、长期决策或阶段管理价值时才写入项目文档。已获授权且边界清楚的实现不需要重复等待批准；出现范围实质扩大、关键假设被推翻、持续异常或新的高风险操作时，应重新规划。

**Execute。** 采用适合当前风险的小批次修改和验证，保持任务边界，保护用户原有修改和未跟踪文件。异常结果应先检查假设和证据，不要连续尝试缺乏依据的补丁。大型日志和原始材料可以保存到适当位置，活动上下文只保留结论、证据位置和下一步。

## 3. 按需文档

文档是任务产生的必要状态，不是开始工作的准入条件。按需构建文档。需要持久化时，文档统一放在实际项目根目录的 `docs/`；仓库已有职责相同且维护良好的结构时，应优先复用。

- `docs/PROJECT.md`：仅在产生需要长期维持的项目愿景、需求、边界、约束或关键决策时创建或更新，不记录执行流水。
- `docs/HANDOFF.md`：仅在任务可能跨会话、需要暂停、阻塞或正式交接时创建或更新，保存当前位置、稳定点、阻碍和下一步。
- `docs/stages/<NN-name>/STATE.md`：仅在任务确有多个阶段，且阶段计划、证据或偏差无法可靠保留在当前上下文时使用。
- `docs/knowledge/`：仅在已经产生经过验证、确实能够迁移到后续任务的高价值经验时使用。

同一信息不应在多份文档中重复维护。长期依据属于 `PROJECT.md`，跨会话恢复信息属于 `HANDOFF.md`，阶段细节属于阶段 `STATE.md`。文档应在有意义的检查点批量更新，而不是在每个微小编辑后写盘。

需要记录跨会话信息时，应区分已验证事实、当前假设、已批准决策、待确认问题和已知阻碍。带有时效性的事实应使用绝对日期并注明来源或确认时间。文档与实际状态冲突时，先检查当前代码、数据和运行结果，再判断应修正实现还是更新过时文档。

## 4. 上下文、暂停与恢复

Agent 应保持一个主要目标，按需读取最相关的代码、文档和知识条目，不应无差别加载整个仓库或全部历史。当上下文已经混入大量过期探索、失败路径或无关输出，并开始影响判断时，应先形成最小交接，再从当前事实重新建立工作上下文。

任务确需跨会话恢复时，交接至少应说明当前状态、最后稳定点、已完成和未完成事项、修改范围、真实验证结果、阻碍、待决问题，以及一至三个可直接执行的下一步。不得保存凭据、令牌或其他敏感信息。

恢复时，先读取适用规则和与当前任务直接相关的交接或项目文档，再检查必要的实际文件、Git 状态、数据或进程。交接内容是定位线索，不是不可质疑的事实；过期内容应先校准再继续。

## 5. 验收、失败处置与 Git

完成声明必须有与任务风险相称的证据。Agent 应对照用户最新要求和实际修改，自主选择必要的测试、构建、检查或人工确认；无法验证的内容应明确说明，不得以推测代替验收。

单项检查失败不等于任务失败。局部、明确且可逆的问题可以继续修复；当根因不明、修改失控、连续修复无效或可能损害用户修改时，应停止写入，保留真实错误和最后稳定点，进行只读诊断，并说明继续修复、保留现场或授权回退所需的条件。未经授权，不得删除、覆盖或回退用户工作区。

需要记录状态时，`fail` 表示验收项未通过，`failed` 表示当前实施路径已经失效，`blocked` 表示正在等待用户决定、授权或外部条件。只有当前范围内没有可行路径，或者用户明确终止时，才将整个任务标记为 `failed` 或 `abandoned`。

如果项目使用 Git，Agent 可以按需要检查状态和差异，以区分本次修改与用户原有内容。是否提交、推送以及如何组织历史，由用户要求和项目惯例决定；不得擅自纳入无关修改、清理工作区或改写历史。

## 6. 子 Agent 与环境能力

子 Agent 不是复杂任务的必需步骤。只有任务可以清晰拆分、并行能够缩短关键路径，或独立复核能够明显降低风险时才使用；同一时间最多使用两个，除非用户明确要求，不得递归派生。默认指定 `luna` 模型并使用 `max` reasoning effort；当前环境不支持时，使用最接近的可用配置并说明。

派发任务时，应说明目标、必要背景、允许范围、不可改变的决策、预期产物和验收标准。并行写文件时应避免所有权重叠；共享项目状态由父 Agent 维护。父 Agent 必须复核会影响主计划的关键证据，并在暂停或完成前收齐正在使用的子任务结果。

路径、Shell、模型、工具和网络条件都应以当前环境为准，不应从旧记录直接推断。Git、pip、npm 或其他下载、上传命令失败时，先判断是否确属超时、DNS、TLS 或连接故障；只有确认是网络层问题时，才可以仅对本次重试临时使用 `127.0.0.1:10808` 代理。不得因认证、权限、参数或版本错误套用代理，也不得把代理写入全局配置或项目文件。

## 7. 高价值经验

阶段结束或最终验收时，Agent 应自主判断是否产生了值得复用的踩坑、根因、诊断路径、约定或洞见。只有经过验证且可能帮助后续任务的内容才需要沉淀；普通执行日志、一次性判断、未经验证的猜测和敏感信息不应进入知识库。

经验可以写入 `docs/knowledge/<topic>.md`，并在条目较多、确有检索需要时建立 `docs/knowledge/INDEX.md`。条目应简要说明适用场景、现象、根因或洞见、推荐做法、验证依据和适用边界。后续 Agent 引用这些经验时仍须重新判断并验证其对当前环境是否适用。

## 8. 沟通与完成

不得为了压缩篇幅省略必要的条件、因果关系和上下文衔接。

任务未完成时，应明确说明当前状态、已经完成的部分、阻碍原因和准确的恢复入口。任务完成时，只持久化对后续维护仍有价值的信息；没有必要的状态或经验时，无需为了结束流程而创建文档。


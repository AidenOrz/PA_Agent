# PA Agent 交接文档

> 审查日期：2026-08-01（基于当前 main 分支工作树，最近提交 `1e651ab`）
> 本文档供接手开发者使用：包含项目概览、架构说明、运行方式、核心业务流程、配置说明，以及**代码审查发现的问题清单与修复优先级**。

---

## 1. 项目概览

PA Agent 是一个 **A股/外汇价格行为(Price Action) AI 量化分析交易桌面程序**：

- PyQt6 图形界面，pyqtgraph 绘制 K 线
- 多数据源接入（MT5 / TradingView / 东方财富 / AkShare / Tushare / yfinance / Baostock）
- 两阶段 LLM 分析（Stage1 方向判断 → Stage2 交易决策），多 AI 提供商（DeepSeek / QClaw / Cursor / WorkBuddy）
- 决策树 + 连续决策追踪 + 交易记录 + 经验沉淀
- 飞书 / PushPlus 消息通知

版本：0.1.0（AGPL-3.0），Python >= 3.11，仅正式支持 Windows（MT5/包内依赖 pywin32）。

---

## 2. 目录结构

```
pa_agent/
├── main.py                 # 入口：崩溃诊断 → 日志 → AppContext.bootstrap → 主窗口
├── app_context.py          # 全局组件装配（settings、数据源工厂、AI client、事件总线）
├── config/
│   ├── settings.py         # 设置加载/保存（含 API key 掩码、脏配置回写）
│   └── paths.py            # 全部路径常量（锚定 PROJECT_ROOT，新增路径必须加在这里）
├── data/                   # 数据源层
│   ├── factory.py          # 数据源工厂（按 settings 选择源）
│   ├── eastmoney_client.py # 东财 HTTP 客户端（主机轮换、节流、信号量）
│   ├── eastmoney_source.py / eastmoney_extended.py / eastmoney_quote.py
│   ├── refresh_loop.py     # 刷新循环（退避、in-flight 保护）
│   ├── snapshot.py         # 快照构建（live frame / analysis frame）
│   ├── tradingview.py      # TV WS 客户端（_snapshot_lock 防并发）
│   ├── mt5.py / akshare_source.py / tushare_source.py / yfinance_source.py / eastmoney_baostock.py
│   └── bar_close_wait.py   # K 线收盘判定/等待收盘倒计时
├── ai/                     # AI 调用与提示词层
│   ├── deepseek_client.py  # OpenAI 兼容客户端（chat/stream_chat、降级重试）
│   ├── router.py           # 提供商路由（deepseek/qclaw/cursor/workbuddy/kkai/packy）
│   ├── qclaw_relay.py / qclaw_relay_manager.py / qclaw_connector.py
│   ├── cursor_connector.py / cursor_sdk_client.py / workbuddy_connector.py
│   ├── session_ledger.py   # 会话/计费台账
│   ├── token_counter.py    # token 估算
│   ├── json_validator.py   # LLM 输出 JSON 校验（jsonschema + 跨字段检查）
│   ├── stage1_normalizer.py / stage2_normalizer.py   # 输出归一化与修复
│   ├── prompt_assembler.py # 提示词组装（K 线特征表、市场特征、程序预填充）
│   ├── pattern_routing.py  # 形态路由
│   ├── decision_tree.py / decision_continuity.py / decision_stance.py  # 决策逻辑
│   ├── coherence_checks.py / trace_normalize.py / trace_semantic_checks.py
│   ├── market_features.py / kline_features.py / trend_context.py / structure_levels.py
│   ├── retry_policy.py / retry_feedback.py / provider_errors.py / mimo_compat.py
│   └── prompts/            # schema 定义
├── orchestrator/           # 分析编排
│   ├── two_stage.py        # 两阶段主流程（含降级、校验重试、取消）
│   ├── free_chat.py        # 自由对话（延续追问）
│   └── validation_retry.py
├── records/                # 记录持久化
│   ├── pending_writer.py   # 分析记录落盘（文件名约定见下）
│   ├── analysis_history.py # 最新记录查找（增量分析锚点）
│   ├── trade_logger.py     # 交易 CSV + K 线截图
│   └── experience_reader.py
├── notify/                 # 飞书 / PushPlus 通知
├── gui/                    # PyQt6 界面（main_window 为核心）
├── indicators/             # atr.py / ema.py（增量计算）
├── util/                   # 日志、事件总线、线程工具、价格 tick、掩码等
├── demo/                   # 演示回放（record_loader / replayer）
└── security/               # 密钥相关
```

其他目录：

- `prompt_engineering/` — 交易策略提示词资产（只读，供 prompt_assembler 加载）
- `config/` — `settings.json`、`feishu.json` 等运行配置（已 gitignore，见 `.gitignore`）
- `records/pending/` — 分析记录（JSON + followups.jsonl）
- `trade_records/` — 交易 CSV 与 K 线截图
- `experience/` — 各形态的成功/失败案例（按 channel 分目录）
- `logs/` — `pa_agent.log`、`crash.log`
- `tests/` — unit / property / integration / e2e（pytest 标记见 `pyproject.toml`）
- `tools/` — 诊断脚本（东财接口诊断、pending 记录分析、密钥审计等）
- `.githooks/pre-commit` — 防提交 settings/日志/记录的钩子

---

## 3. 环境与运行

```bash
# 安装（Windows, Python 3.11+）
pip install -e ".[dev]"

# 启动
python run.py            # 或: python -m pa_agent.main

# 测试
pytest -q                # 或: make test

# Lint（ruff + black）
ruff check . && black --check .   # 或: make lint

# 启用 pre-commit 防密钥/配置入库
make setup-secrets       # powershell tools/setup_git_secrets.ps1
```

**关键环境变量**：API key 类配置优先走环境变量（`DEEPSEEK_API_KEY` 等，`live` 标记测试要求环境变量、绝不读 `config/settings.json`）。tushare 额外支持 `TUSHARE_ADJ` 控制复权。

**CI**：`.github/workflows/ci.yml` 仅做 Windows 安装验证（`pip install -e ".[dev]"` + import 检查），未跑测试。

---

## 4. 核心业务流程

### 两阶段分析（orchestrator/two_stage.py）

```
用户提交分析
  → SnapshotFetchWorker 拉取最新 K 线快照（refresh_loop 维护）
  → _AnalysisWorker（后台 QThread）:
      Stage1: 组装提示词 → LLM 流式输出 → JSON 校验/归一化 → 方向判断
      Stage2: 组装第二段提示词（含 stage1 结果）→ 输出交易决策 JSON
      → 校验失败走 validate_with_retry（语义重试，有上限）
      → 网络错误 → 自动降级到备选提供商（qclaw/cursor/workbuddy）
      → 成功 → 落盘记录 + 写交易 CSV + 截图 + 飞书通知
  → 信号回主线程更新图表/决策树/面板
```

### 关键约定与陷阱

- **记录文件名**：`{YYYY-MM-DD_HH-mm-ss}_{symbol}_{timeframe}.json`，追加 `symbol_timeframe` 维度避免同秒覆盖（⚠ 见已知问题 #1，`%m` 为月份 bug）
- **增量分析锚点**：`analysis_history.find_latest_successful_record` 按目录 mtime 查找上一成功记录，喂给模型做决策连续性
- **所有运行时目录**必须锚定 `config/paths.py:PROJECT_ROOT`，禁止 `Path("trade_records")` 这类依赖 CWD 的相对路径（⚠ 见已知问题 #18）
- **时间处理**：K 线时间戳统一 `datetime_ts.py` 的 ts_open 转换；forming 判断走 `bar_close_wait.py`（含 A 股时段检查）
- **并发模型**：UI 主线程 + RefreshLoop 线程 + 分析 QThread 三路并发；worker 采用三代 ID 失效机制（`_analysis_worker_id` / `_prep_worker_id` / `_snapshot_fetch_id`）+ zombie 回收（`_reap_zombie_workers`）。**新 worker 取消必须走 `_cancel_analysis_worker` 的规范协议**（disconnect 信号 → 失效 ID → wait → 超时登记 zombie），切勿直接置 None 丢弃运行中 QThread
- **日志安全**：API key 一律经 `util/mask_secret.mask_secret` 掩码，日志/记录禁止明文密钥（有 property 测试守护）

---

## 5. 配置说明（config/settings.json）

结构（`pa_agent/config/settings.py`，example 见 `config/settings.example.json`）：

- `provider` — AI 提供商选择与各提供商 key/endpoint/model
- `data_source` — 数据源（mt5/tradingview/eastmoney/akshare/tushare/yfinance）、复权方式
- `analysis` — 周期、K 线数量、风控参数（止损/止盈/仓位）
- `notify` — 飞书/PushPlus 开关与 webhook
- 镜像示例：`config/feishu.example.json`、`config/tv_symbol_aliases.example.json`、`config/exception_state.example.json`

---

## 6. 代码审查发现的问题清单（重点）

> 以下为 2026-08-01 全面审查结论，行号基于当前工作树。按严重程度排序，**未修复**。

### 🔴 严重（数据丢失 / 崩溃，建议尽快修复）

| # | 问题 | 位置 | 影响 | 修复建议 |
|---|------|------|------|----------|
| 1 | 记录文件名 `%m` 写成了月份 | `records/pending_writer.py:33`、`orchestrator/free_chat.py:38` | 同一月内同小时同秒的分析记录互相覆盖；`find_latest_successful_record` 会取到"昨天同一时刻"的记录污染增量分析锚点 | `%H-%m-%S` → `%H-%M-%S`（两处同时改） |
| 2 | 月线 "1M" 被当作 1 分钟 | `data/bar_close_wait.py:10-43`（正则 `re.IGNORECASE` 兜住大写 M，字典无 "1M"） | 月线 forming 判断错误（当月未收盘 K 被判已收盘）、"等待收盘"倒计时 0-60s 循环，影响全部数据源 | 字典加 `"1M": 30*86400` 或正则前特判 |
| 3 | 交易 CSV 非原子读-改-写 | `records/trade_logger.py:607-624` | 并发分析丢行；`"w"` 截断后崩溃整份 CSV 丢失；O(n²) 累积 | 追加模式+表头，或临时文件+`os.replace`，加锁 |
| 4 | 指数 1w/1M 实际返回日线 | `data/eastmoney_source.py:227-229, 271-295` + `eastmoney_client.py:510-525`（klt 硬编码 "101"） | 指数周/月线图表与分析完全错位 | 指数分支按 timeframe 传 klt(102/103)，放大日历窗口 |
| 5 | GUI 丢弃运行中 QThread | `gui/main_window.py:2477-2481`（demo 入口）、`main_window.py:863-882`（快照 worker）、`ai_stream_window.py:658`、`conversation_widget.py:629` | wait 超时后置 None → GC 销毁运行中线程 → 崩溃；demo 入口还叠加旧结果二次触发污染界面 | 统一走 `_cancel_analysis_worker` 协议（disconnect→失效 ID→zombie） |

### 🟠 一般（功能错误 / 体验问题）

| # | 问题 | 位置 | 修复建议 |
|---|------|------|----------|
| 6 | 网络抖动时自动降级**永久改写** settings.json 且不恢复；4xx 业务错误误判为网络错误 | `orchestrator/two_stage.py:1056-1215` | 降级仅本次调用生效（调用后还原配置）；`_is_network_error` 排除 4xx |
| 7 | 校验重试路径的网络异常未捕获，阶段一诊断数据不落盘 | `orchestrator/two_stage.py:541-554, 846-862` | 重试调用外补 try/except，复用首次调用的 partial 保存分支 |
| 8 | WorkBuddy 401/403 被健康检查误报"连接正常" | `ai/workbuddy_connector.py:601-602`（:640-668 分支死代码） | 探针返回 False 或改三态 |
| 9 | stream_chat 对任何异常都走"去 stream_options 重发"，不支持 include_usage 的网关每请求翻倍 | `ai/deepseek_client.py:664-670` | 仅参数不支持类错误降级，其余上抛 |
| 10 | gate_trace 章节排序 key 缺 "1."/"2." 前缀，排序恒失效 | `ai/trace_normalize.py:604-619` | `_CHAPTER_ORDER` 补 `"1.":10, "2.":20` |
| 11 | TradingView 探测 timeout 虚设（ThreadPoolExecutor `shutdown(wait=True)`） | `data/tradingview_connectivity.py:37-40` | 超时打断 socket 或 `shutdown(wait=False)` |
| 12 | 复权设置未贯彻：AkShare 硬编码 qfq、Tushare 默认 qfq，仅东财遵守设置 | `data/akshare_source.py:427,494`、`tushare_source.py:250` | 统一走 `get_kline_adjust()`；另 tushare 指数需 `asset="I"`（:42 的 000300 会失败） |
| 13 | settings.json 非原子写 + 解析失败静默回退默认配置 | `config/settings.py:271-280, 231-235` | 临时文件 + `os.replace`；失败保留坏文件副本并告警 |
| 14 | 字符串价格直接 `f"{entry:.5g}"` 抛 ValueError，导致整次分析展示中断 | `gui/decision_panel.py:510-517` | 加 `float()` 防御（chart_widget 已有先例） |
| 15 | `_probe_workbuddy_api` 与 `_enter_demo_mode` 计时：GUI 线程 `time.sleep(1.5)` 冻结界面 | `gui/main_window.py:1437` | 改 `QTimer.singleShot` 延迟 |
| 16 | 每次保存交易记录重写整份 CSV（效率+并发） | 同 #3 | 同 #3 |
| 17 | token 用量统计漏掉取消/网络错误路径的重试调用 | `orchestrator/two_stage.py:493,637,664,767,799-802` | 统一用 usage_calls 列表累加 |
| 18 | `trade_records` 用相对路径，与全局路径体系不一致（CWD 依赖） | `records/trade_logger.py:29-30`、`ai/decision_continuity.py:12` | 在 `paths.py` 增加 `TRADE_RECORDS_DIR` |

### 🟡 效率问题

| 位置 | 问题 |
|------|------|
| `data/eastmoney_source.py:191-215, 357-399` | 盘中每次快照重复拉取 2 次盘口 + 1 次现价（请求翻倍，易触发限流） |
| `data/yfinance_source.py:109-175`、`akshare_source.py:310-337` | 无缓存，每秒全量重下载历史 + 现价 |
| `ai/deepseek_client.py:466, 632` | 每次请求新建 OpenAI 客户端，无连接复用 |
| `records/analysis_history.py:40-82` | 找最新记录全量 json.loads 每个历史文件（数百 KB~MB），记录多后增量分析卡顿 |
| `ai/prompt_assembler.py:1271-1280` | 同一批 K 线特征重复计算 3+ 次 |
| `gui/decision_flow_viz.py:805, 924-927` | 常驻 60ms 全场景重绘定时器，隐藏也不停，长期耗 CPU |
| `notify/feishu_notifier.py:57-85` | token 刷新在锁内做最长 12s HTTP，并发推送全阻塞 |
| `data/eastmoney_extended.py:799-814` | 裸 requests 无节流，单次分析并发 ~25 请求，易触发限流 |
| `ai/mimo_compat.py:166` | 每请求无条件 deepcopy 大消息 |

### ⚪ 其他小问题

- `ai/decision_continuity.py:221-222`：`return _REL_FLIP if same_structure else _REL_FLIP` 两分支相同，死参数
- `ai/json_validator.py:556-558`：jsonschema 未安装时静默跳过全部校验；:571 `split("'")[1]` 无防御
- `ai/stage2_normalizer.py:1314`：背景限价节点 `bar_range` 硬编码 "K10-K1"，短窗口越界
- `ai/prompt_assembler.py:953-964`：`_load` 失败返回 `"[ERROR: could not load ...]"` 占位文本被拼进提示词
- `data/eastmoney_extended.py:190`：把 Web token 当 User-Agent 发送
- `data/datetime_ts.py:15`：`naive_local_to_utc` 用 `time.timezone`，DST 期间偏移错误
- `records/trade_logger.py:485`：截图文件名秒级精度，同秒两次交易互相覆盖
- `data/tv_symbol_lookup.py:159-163`：模糊子串匹配 len>=2 可能误命中（"中国"→"中国平安"）
- `notify/feishu_notifier.py`：`load_settings` 每次推送都读盘（:92-99）
- `data/ashare_limits.py:61-73`、`eastmoney_extended.py:706-730`：全局缓存无锁（GIL 下低危）
- `records/pending_writer.py:146-152`：`_write_json` 非原子写

---

## 7. 修复优先级建议

1. **P0（一行改动，立即修）**：#1 `%m`→`%M`（两处）
2. **P0**：#3 CSV 原子写、#2 月线 60 秒陷阱
3. **P1**：#5 GUI QThread 引用（3 处）、#6 降级永久改配置、#7 重试丢记录
4. **P1**：#4 指数周/月线、#8 认证误报、#9 请求翻倍
5. **P2**：效率类（东财重复盘口、yfinance/akshare 缓存、analysis_history 全量解析、feishu 锁内刷新）

每修完一项建议跑：`pytest -q`（unit/property 全覆盖）；涉及数据源/两阶段的改动跑 `pytest -m integration`；涉及月线、指数周线、记录文件名的改动需手动回归对应路径。

---

## 8. 开发注意事项

- **禁止提交**：`config/settings.json`、`logs/`、`records/pending/`、`trade_records/`、任何含真实 API key 的文件（pre-commit 钩子 + `tools/setup_git_secrets.ps1` 守护，CI 未跑测试，靠本地钩子把关）
- **新增路径**：一律加到 `config/paths.py` 再引用
- **新增 worker**：遵循三代 ID 失效 + zombie 回收协议（见上文 #4 并发模型）
- **代码风格**：ruff（E/F/I/UP/B/SIM/RUF）+ black，line-length 100
- **加密**：`security/` 与 `util/mask_secret.py` 负责密钥处理，日志输出前必须掩码

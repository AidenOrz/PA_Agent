# 数据源使用文档 — WeStock 主力 + 备份策略

> 编写时间：2026-08-03
> 适用范围：a-share-factor-engine-v6 / AurumQ-RL 本地重建 / 后续 Codex 开发
> 核心结论：**主力数据源 = 用户版 WeStock（腾讯自选股）**，mx-data 有反爬限制仅兜底，AkShare 为第二顺位备份。

---

## 1. 数据源选型结论（一句话版）

```
六类因子数据（资金流/游资/北向/两融/筹码/情绪）
  → 全部用 WeStock 用户版（npx westock-data-skillhub@1.0.5）
  → 实时行情/分时/热搜 补充用内置版 CLI
  → mx-data 仅兜底个股资金流（有严格反爬限制，禁用为主力）
  → AkShare 第二顺位（已封装 fund/lhb/rzrq 三接口）
```

---

## 2. 两个 WeStock 版本详细对比（2026-08-03 实测）

### 2.1 版本现状

| 项目 | 用户版 westockdata | 内置版 westock-data |
|:-----|:------------------|:-------------------|
| 来源 | 市场（marketplace）安装 | WorkBuddy 内置 |
| skill 版本 | **1.0.6**（最新，无更新） | — |
| 底层 npm 包 | **westock-data-skillhub@1.0.5**（最新） | — |
| 调用方式 | `npx -y westock-data-skillhub@1.0.5 <命令>` | `node <app.asar.unpacked>/.../index.js <命令>` |
| 脱离 WorkBuddy 可用 | ✅（需 npm 网络） | ✅（自包含单文件，任意 node ≥18） |
| 依赖 | npm 拉包 | 无依赖，纯 HTTP |

> 版本检查方法：`npm view westock-data-skillhub version`（当前 1.0.5，2026-07-02 发布）
> ⚠️ skill 外壳版本（1.0.6）与 npm 包版本（1.0.5）不同步是设计如此，非落后。

### 2.2 能力对比（实测验证）

| 能力 | 用户版 | 内置版 | 说明 |
|:-----|:------:|:------:|:-----|
| **fund flow 资金流** | ✅ | ✅ | 批量 OK |
| **fund north-holding 北向** | ✅ | ❌ | **用户版独有，关键差异** |
| **fund south-holding 南下** | ✅ | ❌ | 用户版独有 |
| **fund margin 两融** | ✅ | ✅ | 批量不稳定，需逐只 |
| **fund block 大宗** | ✅ | ✅ | — |
| **fund short 卖空** | ✅ | ✅ | 港股/美股 |
| **chip 筹码** | ✅ | ✅ | 批量 45/50 |
| **lhb 龙虎榜（含游资）** | ✅ | ✅ | `--type institution,hotmoney,activeseat,winbuy,winseat` |
| **kline 日期范围** | ✅ | ✅ | 两版都支持 `--start/--end` |
| **quote 实时行情** | ❌ | ⚠️ | 内置版独有，但实测返回空数组 |
| **minute 分时** | ❌ | ✅ | 内置版独有，实测可用 |
| **hot 热搜** | ❌ | ✅ | 内置版独有（stock/wechat），实测可用 |
| **stocklist 股单** | ❌ | ✅ | 内置版独有 |
| **events 事件标签** | ❌ | ✅ | 内置版独有 |
| **dehydrated 脱水研报** | ❌ | ✅ | 内置版独有 |
| **search 日韩股** | ❌ | ✅ | 内置版独有（--market jp/kr） |
| **bond 可转债** | ✅ | ✅ | 用户版文档更全 |
| **sector oper/valuation/forecast/finance** | ✅ | ❌ | **用户版独有，板块经营/估值/预测/财务** |
| **risk 风险事件** | ✅ | ✅ | 用户版更详细（质押/减持/高管 sections） |
| **trade-calendar 交易日历** | ✅ | ❌ | 用户版独有 |
| **disclosure 披露日历** | ✅ | ✅ | — |
| **report detail 研报详情** | ✅ | ✅ | 用户版支持 |
| **esg 评级** | ✅ | ❌ | 用户版独有 |
| **suspension 停复牌** | ✅ | ✅ | — |
| **etf detail/holdings** | ✅ | ⚠️ | 用户版实测可用；内置版 holdings 返回 null |
| **news** | ❌ | ⚠️ | 内置版报"当前渠道不可用" |
| **macro 宏观** | ✅ | ✅ | 用户版支持 expect |

### 2.3 结论：能力矩阵

```
✅ 用户版更强（作为主力）:
  fund north/south-holding（北向/南下，建 hk_* 因子的核心）
  sector oper/valuation/forecast/finance（板块系，建 ind_* 因子）
  bond 可转债 / risk / trade-calendar / esg

✅ 内置版补充（发现类，非主力）:
  minute 分时 / hot 热搜 / stocklist 股单 / events 事件标签

⚠️ 两版都别依赖:
  quote（内置版返回空）/ news（渠道不可用）/ etf holdings（内置版 null）
```

---

## 3. 六类因子数据获取速查（实测命令）

| 因子族 | 数据 | 命令 | 批量 | 备注 |
|:-------|:-----|:-----|:----:|:-----|
| `mf_*` 资金流 | 主力/超大/大单/散户净流入 | `fund flow <code>` | ✅ | 含 5/10/20 日累计 |
| `hm_*` 游资 | 游资名单+买卖个股 | `lhb --type hotmoney` | ✅ | 直接输出"东北猛男/拉萨天团"等 |
| `hk_*` 北向 | 个股季度持仓 | `fund north-holding <code>` | ✅ | 每只 2 季度（最新+次新） |
| `mg_*` 两融 | 融资/融券余额 | `fund margin <code>` | ❌ | 批量不稳定（一次只回1只或null），**必须逐只** |
| `cyq_*` 筹码 | 获利盘/平均成本/集中度 | `chip <code>` | ✅ | 批量 45/50，空返回不稳定需重试 |
| `senti_*` 情绪 | 涨跌分布+情绪文案 | `changedist` | ✅ | 全市场截面；`market-overview --type updown` 备选 |

> **全市场批量建议**：用 `--codes-file` 逗号分隔批量（与 v6 `kline-cache refresh` 同思路），50 只一批。
> **注意事项**：
> - `fund flow` 单股偶尔返回空数组，可带 `--start/--end` 日期参数或重试
> - 筹码空返回会换批变化（疑似数据源覆盖不全），需重试机制
> - 两融只能逐只拉（50 只 = 50 次请求），是最慢的一类

---

## 4. mx-data（东方财富妙想）— 仅兜底

- **严格反爬限制，不可作为主力数据源**（用户确认 2026-07-31）
- 能力：自然语言查询（个股资金流向、行情、财务、股东等）
- 用法：`MX_APIKEY` 环境变量 + `python mx_data.py "自然语言查询"`
- 定位：仅当 WeStock/AkShare 都拿不到时兜底（如个股资金流历史明细）
- 验证历史：2026-06-22 曾验证 ETF 价格 9/9、分红 6/6 全通过，数据本身可信

---

## 5. AkShare — 第二顺位备份

- 已封装 CLI（`akshare-stock-cn` skill）：
  - `fund <code>` 个股资金流向
  - `lhb --date <date>` 龙虎榜
  - `rzrq <code>` 融资融券
- 未封装（有接口但需自己调）：筹码分布（stock_cyq_em）、北向（stock_hsgt）、涨停池
- 数据源依赖网页抓取，接口可能失效；已内置重试

---

## 6. 调用规范（开发约定）

```bash
# 主力：用户版（所有因子数据）
npx -y westock-data-skillhub@1.0.5 <命令> [参数]

# 补充：内置版（实时发现类）
node "C:\Users\Lenovo\AppData\Local\Programs\WorkBuddy\resources\app.asar.unpacked\resources\builtin-skills\westock-data\scripts\index.js" <命令>

# 代码格式：sh600519 / sz000001 / hk00700 / usAAPL
# 批量：逗号分隔（大部分命令支持，两融除外）
# 输出：加 --raw 拿 JSON
```

**Codex 集成建议**：
1. 内置版 index.js 是自包含单文件，建议复制到项目 `vendor/westock-cli/index.js` 固化（WorkBuddy 更新路径可能变）
2. 主力数据获取统一封装 `westock_client.py`（subprocess 调 npx），两融单拉 + 重试机制内置
3. 禁止 web_search / HTTP 直连 / 训练数据替代（skill 铁律）

---

## 7. 已知坑（实测）

| 坑 | 说明 |
|:---|:-----|
| `fund margin` 批量坏 | 一次只回 1 只或 null，必须逐只 |
| `chip` 空返回 | 5/50 左右，换批变化，需重试 |
| `fund flow` 偶发空 | 带日期参数或重试 |
| 内置版 `quote` 空数组 | 命令存在但渠道无数据 |
| 内置版 `news` 不可用 | 报"当前渠道不可用" |
| 内置版 `etf holdings` null | 用用户版代替 |
| 科创板 `--fq bfq` 报错 | 不带 `--fq` 即可（默认不复权） |
| npx 首次调用慢 | 需拉 npm 包，之后有缓存 |
